import { BrowserRouter } from "react-router-dom";
import Router from "./Router";
const root =()=>{
    return(
    <BrowserRouter>
        <Router/>
    </BrowserRouter>
    
    )
}
export default root;